#include <stdio.h>
void main() {

unsigned int a;  // Declare variable "a" of type int
a = -1;          // Assign "a" the value -1

// print the variable using %u specifier (%u used to print the unsigned values)
printf("%u\n", a);

}